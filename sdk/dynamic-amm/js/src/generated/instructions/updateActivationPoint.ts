/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { AMM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_ACTIVATION_POINT_DISCRIMINATOR = new Uint8Array([
  150, 62, 125, 219, 171, 220, 26, 237,
]);

export function getUpdateActivationPointDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ACTIVATION_POINT_DISCRIMINATOR
  );
}

export type UpdateActivationPointInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountPool extends string | IAccountMeta<string> = string,
  TAccountAdmin extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            IAccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateActivationPointInstructionData = {
  discriminator: ReadonlyUint8Array;
  newActivationPoint: bigint;
};

export type UpdateActivationPointInstructionDataArgs = {
  newActivationPoint: number | bigint;
};

export function getUpdateActivationPointInstructionDataEncoder(): Encoder<UpdateActivationPointInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newActivationPoint', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_ACTIVATION_POINT_DISCRIMINATOR,
    })
  );
}

export function getUpdateActivationPointInstructionDataDecoder(): Decoder<UpdateActivationPointInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newActivationPoint', getU64Decoder()],
  ]);
}

export function getUpdateActivationPointInstructionDataCodec(): Codec<
  UpdateActivationPointInstructionDataArgs,
  UpdateActivationPointInstructionData
> {
  return combineCodec(
    getUpdateActivationPointInstructionDataEncoder(),
    getUpdateActivationPointInstructionDataDecoder()
  );
}

export type UpdateActivationPointInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  /** Pool account (PDA) */
  pool: Address<TAccountPool>;
  /** Admin account. */
  admin: TransactionSigner<TAccountAdmin>;
  newActivationPoint: UpdateActivationPointInstructionDataArgs['newActivationPoint'];
};

export function getUpdateActivationPointInstruction<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof AMM_PROGRAM_ADDRESS,
>(
  input: UpdateActivationPointInput<TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress }
): UpdateActivationPointInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.pool), getAccountMeta(accounts.admin)],
    programAddress,
    data: getUpdateActivationPointInstructionDataEncoder().encode(
      args as UpdateActivationPointInstructionDataArgs
    ),
  } as UpdateActivationPointInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin
  >;

  return instruction;
}

export type ParsedUpdateActivationPointInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool account (PDA) */
    pool: TAccountMetas[0];
    /** Admin account. */
    admin: TAccountMetas[1];
  };
  data: UpdateActivationPointInstructionData;
};

export function parseUpdateActivationPointInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUpdateActivationPointInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      admin: getNextAccount(),
    },
    data: getUpdateActivationPointInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
