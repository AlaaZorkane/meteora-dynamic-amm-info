/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { AMM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getCurveTypeDecoder,
  getCurveTypeEncoder,
  type CurveType,
  type CurveTypeArgs,
} from '../types';

export const OVERRIDE_CURVE_PARAM_DISCRIMINATOR = new Uint8Array([
  98, 86, 204, 51, 94, 71, 69, 187,
]);

export function getOverrideCurveParamDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OVERRIDE_CURVE_PARAM_DISCRIMINATOR
  );
}

export type OverrideCurveParamInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountPool extends string | IAccountMeta<string> = string,
  TAccountAdmin extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            IAccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type OverrideCurveParamInstructionData = {
  discriminator: ReadonlyUint8Array;
  curveType: CurveType;
};

export type OverrideCurveParamInstructionDataArgs = {
  curveType: CurveTypeArgs;
};

export function getOverrideCurveParamInstructionDataEncoder(): Encoder<OverrideCurveParamInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['curveType', getCurveTypeEncoder()],
    ]),
    (value) => ({ ...value, discriminator: OVERRIDE_CURVE_PARAM_DISCRIMINATOR })
  );
}

export function getOverrideCurveParamInstructionDataDecoder(): Decoder<OverrideCurveParamInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['curveType', getCurveTypeDecoder()],
  ]);
}

export function getOverrideCurveParamInstructionDataCodec(): Codec<
  OverrideCurveParamInstructionDataArgs,
  OverrideCurveParamInstructionData
> {
  return combineCodec(
    getOverrideCurveParamInstructionDataEncoder(),
    getOverrideCurveParamInstructionDataDecoder()
  );
}

export type OverrideCurveParamInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  /** Pool account (PDA) */
  pool: Address<TAccountPool>;
  /** Admin account. */
  admin: TransactionSigner<TAccountAdmin>;
  curveType: OverrideCurveParamInstructionDataArgs['curveType'];
};

export function getOverrideCurveParamInstruction<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof AMM_PROGRAM_ADDRESS,
>(
  input: OverrideCurveParamInput<TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress }
): OverrideCurveParamInstruction<TProgramAddress, TAccountPool, TAccountAdmin> {
  // Program address.
  const programAddress = config?.programAddress ?? AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.pool), getAccountMeta(accounts.admin)],
    programAddress,
    data: getOverrideCurveParamInstructionDataEncoder().encode(
      args as OverrideCurveParamInstructionDataArgs
    ),
  } as OverrideCurveParamInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin
  >;

  return instruction;
}

export type ParsedOverrideCurveParamInstruction<
  TProgram extends string = typeof AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool account (PDA) */
    pool: TAccountMetas[0];
    /** Admin account. */
    admin: TAccountMetas[1];
  };
  data: OverrideCurveParamInstructionData;
};

export function parseOverrideCurveParamInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedOverrideCurveParamInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      admin: getNextAccount(),
    },
    data: getOverrideCurveParamInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
